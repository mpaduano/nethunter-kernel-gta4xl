name: Build Overclocked Nethunter Kernel (Android 16)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential flex libssl-dev libcrypto++-dev ccache libncurses-dev git zip unzip curl libgmp-dev libmpfr-dev libmpc-dev gcc-aarch64-linux-gnu gcc-arm-linux-gnueabihf clang-19 lld-19 llvm-19

      - name: Clone Kernel Source
        run: |
          git clone --depth=1 -b lineage-22.2 https://github.com/LineageOS/android_kernel_samsung_gta4xl.git $GITHUB_WORKSPACE/kernel

      - name: Pre-Build Header Patches
        run: |
          cd $GITHUB_WORKSPACE/kernel
          # 1. Fix double void syntax
          sed -i 's/static inline void void/static inline void/g' include/linux/nmi.h
          # 2. Add ZONE_DMA to mmzone.h to fix compilation error
          sed -i '/#define _LINUX_MMZONE_H/a #define ZONE_DMA 0' include/linux/mmzone.h

      - name: Disable tzdev Driver
        run: |
          cd $GITHUB_WORKSPACE/kernel
          # Disable tzdev driver to avoid missing firmware file errors
          if [ -f arch/arm64/configs/exynos9611-gta4xlwifi_defconfig ]; then
            sed -i 's/CONFIG_TZDEV=y/CONFIG_TZDEV=n/g' arch/arm64/configs/exynos9611-gta4xlwifi_defconfig
          fi

      - name: Inject Overclock
        run: |
          cd $GITHUB_WORKSPACE/kernel
          TARGET_FILE="arch/arm64/boot/dts/samsung/exynos9611-gta4xlwifi_eur_open_gpio_05.dtsi"
          if [ -f "$TARGET_FILE" ]; then
            sed -i 's/2301000/2405000/g' "$TARGET_FILE"
            sed -i 's/1150000/1225000/g' "$TARGET_FILE"
          fi

      - name: Compile the Kernel
        run: |
          cd $GITHUB_WORKSPACE/kernel
          
          # Environment variables
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export LLVM=1
          export LLVM_IAS=1
          export CLANG_TRIPLE=aarch64-linux-gnu-
          
          # Configure
          make ARCH=arm64 exynos9611-gta4xlwifi_defconfig
          
          # KCFLAGS: 
          # -std=gnu89: Forces the compiler to use the older standard that matches your kernel source
          # -Wno-error: Ignores compilation warnings that would otherwise stop the build
          # -fno-integrated-as: Uses GNU assembler for compatibility
          export KCFLAGS="-w -Wno-error -std=gnu89 -fno-integrated-as"
          
          # Build
          make -j$(nproc) ARCH=arm64

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Android-16-Kernel-Build
          path: kernel/arch/arm64/boot/Image.gz-dtb
